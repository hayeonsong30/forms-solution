# 폼솔루션 다음 작업 메모

작성일: 2026-08-14
최종 동기화일: 2026-08-19

> 이 문서는 현재 프로토타입 이후의 실제 구현 작업만 `다음 작업`으로 취급한다. 아래 `해결 완료`, `확정`, `구현 완료` 절은 결정 이력이며 다시 논의할 작업 목록이 아니다.

## 작업 목적 — 최우선 원칙

현재 만드는 프로토타입은 단순한 디자인 시안이나 발표용 목업이 아니다.

- 프로토타입 화면을 기준으로 상세 PRD를 작성한다.
- 완성된 상세 PRD와 화면을 Claude Code에 제공하여 실제 제품을 바이브코딩한다.
- 따라서 화면에 보이는 열, 상태, 버튼, 명칭, 활성 조건, 이동 경로는 실제 구현 요구사항으로 취급한다.
- 임시 장식이나 동작하지 않는 버튼을 불필요하게 추가하지 않는다.
- 각 기능은 정상 흐름뿐 아니라 빈 상태, 처리 중, 오류, 완료, 권한과 비활성 조건까지 정의한다.
- 프로토타입을 수정할 때 관련 PRD와 다음 작업 메모도 함께 갱신한다.
- PRD에는 개발자가 추가 해석 없이 구현할 수 있도록 화면 ID, 기능 ID, 입력값, 출력값, 상태 전이, 검증, 예외, 수용 기준을 기록한다.
- 데모용 모의 동작과 실제 제품 연결 대상(API, Gemini, NCode, 스마트펜 SDK)을 명확히 구분한다.

최종 작업 흐름:

```text
프로토타입 화면·상호작용 확정
→ 화면별 상세 PRD 작성
→ 데이터·상태·예외·수용 기준 확정
→ Claude Code 바이브코딩 입력 자료 구성
→ 실제 구현·검증
```

## 해결 완료된 설계 질문

### 1. 반복행과 중복된 필드명의 차이 — 해결 완료

다음 내용을 사례 중심으로 정리한다.

- 같은 라벨이 여러 번 등장하는 일반 중복 필드
- 하나의 행 구조가 여러 번 반복되는 반복행
- 두 경우의 내부 ID, 데이터 키, JSON 구조 차이
- `item_code`, `item_code_2` 방식과 `inspection_rows[].item_code` 방식의 차이
- 시스템이 반복행을 자동 추천하는 판단 기준
- 사용자가 반복행을 생성·재편집할 때 필요한 편집 UX. 반복행 해제는 후속 범위
- 반복행 OCR 결과를 CSV·Excel로 펼치는 방식

검토 예시:

```text
일반 중복
품목코드 → item_code
품목코드 → item_code_2

반복행
검사내역[]
├─ 품목코드 → inspection_rows[].item_code
├─ 수량     → inspection_rows[].quantity
└─ 판정     → inspection_rows[].result
```

### 2. 양식이 매번 다른데 데이터 키를 시스템이 어떻게 자동 생성하는가 — 정책 확정

다음 항목을 정책과 기술 흐름으로 정리한다.

- 한국어·일본어 라벨을 영문 데이터 키로 변환하는 방식
- 사전 매핑, 규칙 기반 변환, AI 번역 중 무엇을 우선할지
- 같은 표현과 유사 표현을 동일한 표준 키로 연결할지
- 번역이 애매하거나 라벨이 없는 영역의 키 생성 방식
- 중복 키 자동 번호 부여 규칙
- 반복 그룹 내부의 경로 자동 생성
- 고객사마다 용어가 다를 때 사용자 사전 또는 표준 용어집 운영 방식
- 키 생성 신뢰도와 사용자 확인이 필요한 조건
- 템플릿 배포 전·후 키 변경 정책
- 기존 CSV/API 연동을 보호하는 버전 정책

검토할 기본 파이프라인:

```text
필드 영역 및 주변 라벨 감지
→ 원문 라벨 추출
→ 표준 용어 사전 조회
→ 규칙 기반 키 후보 생성
→ 필요할 때 AI가 의미 기반 영문 키 추천
→ 중복·예약어·경로 검사
→ 최종 키 자동 확정 또는 사용자 확인
```

## 현재까지 정한 방향

- 사용자는 필드 유형을 선택하고 필드명을 입력하며, 시스템이 데이터 키를 자동 제안한다.
- 내부 ID는 시스템이 자동 발급하며 사용자에게 읽기 전용으로만 노출한다.
- 데이터 키도 시스템이 자동 생성한다.
- 데이터 키는 현행 오른쪽 `기본 정보`에서 편집 완료 전까지 수정할 수 있다.
- 좌표는 문서 위 영역 배치 결과로 자동 저장한다.
- 템플릿이 배포되거나 데이터가 수집된 이후에는 데이터 키를 자동 변경하지 않는다.
- 배포 이후 구조 변경은 템플릿 버전 업으로 처리한다.
- 라벨, 내부 ID, 데이터 키, 입력 타입, 영역 좌표는 내부 데이터 모델에서 분리한다.
- OCR 원문과 정규화 값, 최종 확정 값도 다음 단계에서 분리 저장한다.

## 현재 프로토타입 상태

실행 주소: http://127.0.0.1:4173/

주요 파일:

- `index.html`
- `styles.css`
- `settings.css`
- `app.js`

현재 구현 기능:

- 대시보드, 양식 관리, 문서 조회의 3개 주 메뉴
- 고객사·시스템 관리자 대시보드 시각 예시
- 양식 관리 Printable / Not Printable 테이블과 상태별 행 진입
- 스마트펜 데이터 가져오기 창과 선택 페이지·전체 페이지 등록 모의 동작
- 인쇄·작성·완료 상태별 문서 상세과 OCR·확정·출력 모의 흐름
- 텍스트, 숫자, 날짜, 시간, 체크 판정, 단일·다중 선택 필드
- 반복 그룹 구조
- 클릭 및 드래그 필드 추가
- 필드 이동·크기 조절·복사·붙여넣기·삭제·다중 선택
- 왼쪽 문서 필드 목록의 검색·선택·복사·삭제·순서 변경
- 선택 옵션별 판정 영역 생성·이동
- 반복 그룹 생성과 재편집, 첫 행 높이 자동 계산
- 필드 기본 정보·유형별 설정·위치 및 크기
- 빈 PDF 업로드 상태와 Printable 양식 조회 전용 상태
- AI 필드 추천 모의 동작
- 데이터 테스트의 빈 JSON·CSV·기본 Excel 구조 확인
- 데이터 키 자동 임시 생성

현행에서 제외된 UI:

- 글로벌 설정, 별도 레이어, 필드별 OCR·위치 탭
- 잠금·숨김, 자동 정렬·같은 크기·간격 맞춤
- 서명·이미지·바코드·도장·계산 필드
- 반복행 해제와 사용자가 입력하는 행 높이

Desktop 동기화 위치:

`/Users/neolab/Desktop/forms solution/prototype/`

## 다음 작업 권장 순서

1. Claude Code 프로젝트 기반·DB·인증·고객사 범위 구성
2. 프로토타입의 3개 메뉴와 상태 전이를 실제 저장 데이터에 연결
3. PDF 업로드·렌더링·정규화 좌표·필드 편집기를 실제 구현
4. 템플릿·필드·선택 옵션·반복 그룹 저장과 자동 저장·복구 구현
5. 서버 Gemini API와 양식 자동 추천 작업 연결
6. 스마트펜 SDK 어댑터 또는 샘플 파일 가져오기 연결
7. 필기 원본 정렬·필드별 OCR·검수·최종 확정 구현
8. 확정 JSON에서 CSV·기본 Excel 파일 생성
9. 고객사 대시보드와 목록을 실제 집계 API에 연결
10. Kotobuki·ITSUWA E2E 및 권한·오류·재시도 검증

## 다음 대화 시작 문구

`폼솔루션 다음 작업 이어서 해줘. NEXT_다음작업.md부터 확인해줘.`

## 2026-08-18 대시보드 확정 이력

현재 프로토타입에는 `고객사 / 시스템 관리자` 대시보드 전환 화면과 AI OCR 사용량 예시가 구현되어 있다.

아래 검토사항은 2026-08-19 프로토타입과 전체 PRD에 반영 완료했다.

1. 상단 KPI 카드 재구성
   - 현재 카드 수가 많고 설명이 길어 가독성이 낮다.
   - 핵심 숫자만 3~4개로 축소하거나 `전체 / 활성`처럼 연관 지표를 한 카드에 묶는다.
   - 누적 지표와 선택 기간 지표를 시각적으로 분리한다.
2. 고객사 대시보드 본문 재배치
   - 현재 `문서 처리 현황` 영역은 제거 후보로 검토한다.
   - 해당 넓은 영역에 `최근 문서` 테이블을 배치한다.
   - 최근 문서에서 상태, 작성자, 최근 일시, 조회 행동을 바로 제공한다.
3. 많이 사용하는 대상 표시
   - 고객사 대시보드: 문서 처리량이 많은 사용자, 자주 사용하는 양식 순위를 검토한다.
   - 시스템 관리자 대시보드: AI OCR 페이지 또는 처리 문서가 많은 고객사 순위를 제공한다.
   - 단순 로그인 횟수보다 실제 업무량을 기준으로 순위를 산정한다.
   - 기간 필터에 따라 순위가 변경되어야 한다.
4. 날짜 필터 실제 동작 범위 확정
   - 오늘 / 최근 7일 / 최근 30일 / 이번 달 / 직접 설정
   - 문서, 사용자 활동, AI OCR 사용량, 순위에 동일 기간을 적용한다.
   - 전체 등록 사용자와 활성 계정처럼 현재 시점 지표는 기간 필터에서 제외한다.

권장 순위 지표 초안:

```text
고객사 대시보드
- 처리 문서가 많은 사용자 TOP 5
- 많이 사용한 양식 TOP 5

시스템 관리자 대시보드
- AI OCR 사용량이 많은 고객사 TOP 5
- 처리 문서가 많은 고객사 TOP 5
```

주의: 사용자·고객사 순위는 감시나 성과 평가로 오해되지 않도록 `우수 사용자`가 아닌 `사용량 상위`로 표현한다.

## 2026-08-18 추가 UI 수정 요청 — 구현 완료 이력

### 1. 양식 관리

- `편집 미완료`인 Not Printable 샘플 행을 추가한다.
- 편집 미완료 행은 행 전체를 눌러 편집 화면으로 이동하고 작업 열에는 비활성 `프린트`만 표시한다.
- `인쇄 가능` 행은 편집할 수 없고 프린트만 가능하게 유지한다.
- 테이블에서 편집 중과 인쇄 가능 상태를 색상과 버튼으로 명확히 구분한다.

### 2. 양식 편집 화면

- 선택 필드의 옵션별 영역을 PDF 위에서 직접 드래그해 지정할 수 있게 한다.
- 단일·다중 선택 필드는 각 옵션이 독립적인 좌표 영역을 가진다.
- 옵션 목록 선택과 PDF 영역 선택 상태를 동기화한다.
- 필드 복사·붙여넣기 단축키를 지원한다.
  - macOS: `Command+C`, `Command+V`
  - Windows: `Ctrl+C`, `Ctrl+V`
  - 기존 `Command/Ctrl+D` 복제 기능과 역할을 정리한다.
- 필드 삭제는 `Delete`와 `Backspace`를 유지한다.
- 복사·삭제 버튼을 속성 패널 하단에만 두지 않고 선택 필드 근처의 빠른 도구 또는 상단 도구 모음에도 제공한다.
- 편집 화면 상단에서 양식 이름을 수정할 수 있게 한다.
- `폼 테스트` 명칭을 `데이터 테스트`로 변경한다.

#### 검수 필요 표시 검토

- 기능 자체를 완전히 삭제하지 않는다.
- 편집 화면에서는 `검수 필요` 집계와 강조를 숨기거나 최소화할 수 있다.
- 실제 필기 OCR 결과 화면에서는 필수값 누락, 형식 오류, 선택 충돌, 인식 불확실 항목을 찾기 위해 유지한다.
- 프로토타입에서 AI 신뢰도 값을 실제로 제공하지 못하면 `저신뢰도` 대신 `확인 필요`로 단순 표시한다.

### 3. 문서 조회 상태별 동작

#### 인쇄 상태 문서

- `조회`를 누르면 문서 상세 화면을 연다.
- 원본 양식과 정의된 필드 영역을 표시한다.
- 필기와 OCR 값은 없는 상태로 표시한다.
- 필드값 입력란은 비어 있고 조회 전용으로 제공한다.
- `필기 데이터가 아직 등록되지 않았습니다` 안내를 표시한다.
- OCR 실행, 값 수정, 다운로드는 비활성화한다.

#### 작성 상태 문서

- `조회`를 누르면 필기 원본 이미지 또는 스마트펜 획 합성 화면을 표시한다.
- 초기 상태에서는 필기 원본만 보이고 OCR 값은 아직 없을 수 있다.
- `AI OCR 실행` 버튼을 제공한다.
- OCR 실행 중 진행 상태를 표시한다.
- OCR 완료 후 결과를 `인식된 필드값`에 자동 적용한다.
- 원본 인식값과 사용자가 수정한 최종값을 구분해 저장한다.
- OCR 완료 후 값 수정과 최종 확정을 가능하게 한다.

권장 상태 흐름:

```text
작성·OCR 전
→ AI OCR 실행
→ 처리 중
→ OCR 완료·확인 필요
→ 값 수정
→ 최종 확정
→ 완료
```

### 4. 문서 상세 다운로드 위치

- JSON, CSV, 기본 Excel 다운로드 버튼을 화면 오른쪽 상단으로 이동한다.
- 최종 확정 전에는 버튼을 비활성화하거나 `미리보기`만 허용한다.
- 최종 확정 후 다운로드 버튼을 활성화한다.
- 주요 작업 순서는 `저장 → 최종 확정 → 다운로드`가 명확히 보이게 한다.

### 5. 당시 구현 순서 — 2026-08-19 완료

1. 양식 관리 편집 중 행과 상태별 버튼
2. 양식명 인라인 편집과 `데이터 테스트` 명칭 변경
3. 필드 복사·붙여넣기·삭제 빠른 도구
4. 선택 옵션별 영역 드래그
5. 인쇄 상태 문서 조회 전용 화면
6. 작성 상태의 OCR 실행 전·처리 중·완료 화면
7. 다운로드 버튼 위치와 확정 전후 활성 조건
8. 대시보드 카드와 최근 문서·사용량 순위 개편

### 2026-08-19 구현 완료

- 양식 관리에 `편집 미완료` Not Printable 샘플 행과 행 클릭 편집 동작 추가
- 인쇄 가능 양식의 편집 잠금 유지
- 편집 화면 양식명 직접 수정
- `폼 테스트`를 `데이터 테스트`로 변경
- 상단 필드 복사·붙여넣기·삭제 빠른 도구 추가
- Command/Ctrl+C, Command/Ctrl+V 단축키 추가
- 선택 옵션 영역 생성 후 직접 이동 기능 추가
- 인쇄·작성·완료 문서의 상태별 조회 화면 분기
- 작성 문서의 AI OCR 실행 전·처리 중·결과 적용 흐름 추가
- 최종 확정 후 JSON·CSV·기본 Excel 활성화
- 다운로드 기능을 문서 상세 오른쪽 상단으로 이동
- 고객사 대시보드 KPI 4개 결합 카드로 단순화
- 문서 처리 현황 그래프를 최근 문서 표로 교체
- 고객사 사용량 상위 사용자·양식 추가
- 시스템 관리자 AI OCR·문서 처리량 상위 고객사 추가
- 전체 PRD와 양식 편집기 상세 PRD 동기화
- 양식 프린트 상태를 Printable / Not Printable로 단순화
- Not Printable 사유를 편집 미완료 / 사용자 프린트 중지로 구분
- 완료 양식에서 프린트 상태를 사용자가 직접 변경하는 동작 추가
- 선택 옵션 영역 이동이 막히던 스타일 우선순위 수정
- 데이터 테스트를 빈 양식·빈 값·JSON/CSV/Excel 구조 확인 화면으로 변경
- 데이터 테스트에서는 순서 변경을 제공하지 않고 편집 화면 필드 목록에서만 변경하도록 정책 확정
- 양식 관리 작업 열을 프린트 버튼만 남기고 행 클릭으로 편집·조회 상세 진입
- Printable 양식은 조회 전용 편집 화면으로 열고 모든 변경 도구 잠금
- 왼쪽 문서 필드 목록 드래그 순서 변경 구현
- 변경된 필드 순서를 데이터 테스트와 JSON·CSV·Excel 열 순서에 반영

## 2026-08-19 실제 AI 연결 다음 작업

- 전체 PRD `7.7.7~7.7.20`의 실제 구현용 AI 파이프라인을 기준으로 개발한다.
- 1차는 Gemini 단독 + 서버 검증으로 완성하고 Document AI 실제 연동은 정확도 평가 후 결정한다.
- API 키는 서버 환경 변수에만 저장하며 브라우저에서 Gemini를 직접 호출하지 않는다.
- Mock AI로 화면·DB 저장을 먼저 연결한 뒤 실제 Gemini 호출로 전환한다.
- 양식 자동 추천과 필기 OCR을 서로 다른 AI 작업 유형으로 구현한다.
- 일반 필드는 페이지당 최대 12개, ITSUWA 반복행은 기본 5행 단위 묶음 OCR로 시작한다.
- Gemini 자체 신뢰도만 사용하지 않고 필수값·유형·범위·선택 충돌 등의 구체적인 검수 사유를 생성한다.
- 원 OCR 값, 정규화 값, 사용자 최종값을 분리해서 저장한다.
- 최종 확정 JSON 하나에서 CSV와 기본 Excel을 생성한다.
