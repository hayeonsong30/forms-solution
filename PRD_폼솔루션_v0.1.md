# 폼솔루션 PRD v0.1

- 문서 상태: 1차 데모 범위 수정안
- 작성일: 2026-08-13
- 현행 프로토타입 동기화일: 2026-08-19
- 대상: 데모 → 파일럿 MVP → 정식 제품
- 기준 자료: 요구사항정의서 2종, DigiDox 화면, GoCanvas 벤치마킹, Kotobuki·ITSUWA 샘플 양식

## 1. 제품 정의

기존 종이 양식을 그대로 사용하면서 NCode와 스마트펜으로 작성된 필기를 수집하고, AI OCR과 사용자 검수를 거쳐 구조화 데이터 및 PDF·CSV·Excel로 내보내는 멀티테넌트 폼 운영 서비스다.

핵심 가치는 다음과 같다.

1. 고객사가 이미 쓰는 PDF 양식을 새로 만들지 않고 그대로 디지털화한다.
2. 스마트펜 필기와 원본 양식의 위치를 정확히 연결한다.
3. 반복행을 포함한 필기 내용을 검색·집계 가능한 데이터로 변환한다.
4. 원본 증빙 PDF와 업무용 CSV·Excel을 함께 제공한다.
5. 양식 변경 후에도 과거 인쇄물과 데이터를 올바른 버전으로 해석한다.

## 2. 제품 원칙

- 데모를 별도 구조로 폐기하지 않는다. 템플릿, 버전, 필드, 반복 그룹, 문서 상태 모델은 정식 제품 구조를 사용한다.
- 프린터·USB 수집은 제조사 SDK, 통신 규격 또는 참고 Git 저장소가 확보되면 1차 데모부터 실제 연동을 우선한다. 외부 의존성이 준비되지 않은 구간만 교체 가능한 어댑터와 샘플 데이터로 대체한다.
- AI OCR은 실제 API 키를 연결해 동작시키되, 키는 브라우저에 노출하지 않고 서버에서만 보관·호출한다.
- 화면에 보이는 필드명과 데이터 식별자를 분리한다.
- OCR 결과는 자동 확정하지 않고 원본 필기와 함께 검수할 수 있어야 한다.
- 단건 증빙과 다건 데이터 분석의 출력 목적을 구분한다.

## 3. 사용자와 권한

### 3.1 시스템 관리자

- 고객사와 파트너사를 생성·관리한다.
- 공통 NCode 자원, 시스템 상태, 저장소, 사용량과 장애 로그를 관리한다.
- 고객사 데이터에는 지원 목적의 제한된 접근만 허용한다.

### 3.2 고객사 관리자

- 자사 양식 업로드, 필드 편집, 버전 배포, 인쇄 설정을 담당한다.
- 사용자와 권한, 출력 형식, OCR 설정을 관리한다.
- 자사 문서와 처리 현황을 조회한다.

### 3.3 현장 사용자·검수자

- 인쇄된 양식에 스마트펜으로 작성한다.
- 업로드된 필기와 OCR 결과를 조회·수정·확정한다.
- 권한 범위에서 PDF·CSV·Excel을 내려받는다.

> 시스템 관리자용 운영 콘솔과 고객사용 서비스 콘솔은 분리하되, 고객사 관리자와 현장 사용자는 같은 고객 콘솔 안에서 권한에 따라 메뉴가 달라지는 구조를 권장한다.

## 4. 대표 양식과 검증 목적

아래 양식은 모두 1차 데모에 탑재하기 위한 목록이 아니다. 편집기 상세 기능을 빠짐없이 도출하기 위한 **설계 검증 세트**이며, 1차 데모에서는 우선순위가 높은 2종을 실제 시나리오로 구현한다.

| 양식 | 대표 사용 장면 | 핵심 필드 구조 | 반드시 검증할 기능 |
|---|---|---|---|
| 시설 이용 신청서(Kotobuki) | 이용자가 시설 사용을 신청하고 담당자가 승인 | 텍스트, 날짜·시간, 라디오, 체크, 장문, 동의, 서명 | 일반 필드 편집, 다중 페이지, 필수값, 개인정보, 서명, 합성 PDF |
| 입고·품질 검사표(ITSUWA) | 입고된 여러 품목/LOT를 한 장에서 검사 | 문서 기본정보 + 최대 25개 반복행 | 반복행 정의, 행 분리 OCR, 행 추가·삭제, CSV 평탄화, Excel 목록 출력 |
| 설비 점검 체크리스트 | 설비별 정상/이상 여부와 조치 내용을 기록 | 체크·라디오, 수치, 조건부 상세, 서명 | 선택값 인식, 조건부 필드, 허용범위 검증, 이상 항목 필터링 |
| 건설·현장 일보 | 날짜별 인원·장비·자재·작업 내용을 보고 | 여러 반복 그룹, 장문, 날씨, 승인 서명 | 한 문서 내 복수 반복 그룹, 장문 OCR, 집계, 단계별 승인 |
| 배송·인수 확인서 | 배송 품목을 확인하고 수령인이 서명 | 거래처 정보, 품목 반복행, 수량, 인수 서명 | 일련번호, 반복행, 수량 합계, 수령 서명, 단건 PDF·다건 CSV |
| 현장 서비스 작업지시서 | 방문 작업, 사용 부품, 작업 시간과 고객 확인 기록 | 작업 내역, 부품 반복행, 시작/종료 시각, 서명 | 시간 계산, 반복행, 필수 항목, 완료 잠금, 고객용 결과 PDF |

의료 문진·동의서는 민감정보와 보존 정책 검증에 유용하지만 데모 대표 양식에서는 제외하고, 정식 제품의 보안·컴플라이언스 검토 사례로 둔다.

### 4.1 1차 데모 적용 범위

| 구분 | 양식 | 데모 적용 수준 |
|---|---|---|
| 핵심 시나리오 A | Kotobuki 시설 이용 신청서 | PDF 업로드부터 일반 필드 편집, OCR 검수, 확정, JSON·CSV·기본 Excel까지 완전 구현 |
| 핵심 시나리오 B | ITSUWA 입고·품질 검사표 | 반복행 편집, 행 단위 OCR·수정, JSON 배열·행 단위 CSV·기본 Excel까지 완전 구현 |
| 편집기 검증용 | 설비 점검 체크리스트 | 체크·라디오·숫자 범위 기능 정의와 테스트 데이터만 반영 |
| 후속 검증용 | 현장 일보·배송 확인서·작업지시서 | 복수 반복 그룹, 조건부 필드, 합계·시간 계산, 승인 요구사항 도출에 사용 |

따라서 1차 데모는 6개 양식을 얕게 보여주지 않고, 2개 양식의 전체 업무 흐름을 끝까지 수행하는 데 집중한다. 다만 편집기의 내부 모델은 나머지 양식에서 도출한 기능을 나중에 추가할 수 있어야 한다.

## 5. 핵심 사용자 흐름

1. 고객사 관리자가 빈 양식 PDF를 업로드한다.
2. 시스템이 페이지, 문자, 표와 입력 후보 영역을 분석한다.
3. 관리자가 일반 필드와 반복행 영역을 확인·수정한다.
4. 필드명, 데이터 키, 입력 형식, 필수 여부, OCR 방식을 설정한다.
5. 템플릿 버전을 저장하고 NCode 패턴과 연결한 뒤 활성화한다.
6. 사용자가 양식을 NCode와 함께 인쇄한다.
7. 현장에서 스마트펜으로 작성한다.
8. USB 수집기가 펜 데이터를 읽어 서버에 업로드한다.
9. 시스템이 NCode로 고객사·템플릿 버전·문서·페이지를 판별한다.
10. 필기 좌표를 필드 영역과 매핑하고 AI OCR을 실행한다.
11. 검수자가 원본 필기와 인식값을 나란히 보고 수정한다.
12. 검증 오류를 해소한 뒤 문서를 최종 확정한다.
13. 확정 데이터를 JSON으로 확인하고 동일한 데이터로 CSV와 기본 Excel을 자동 생성한다.
14. 고객 맞춤 Excel 템플릿과 PDF·Excel 고급 출력은 후속 단계에서 연결한다.

## 6. 정보 구조와 주요 화면

### 고객사 콘솔

1. 대시보드: 고객사 사용자·양식·문서 KPI, 최근 문서, 빠른 실행, AI OCR 사용량, 사용자·양식 순위
2. 양식 관리: 템플릿 목록, 버전, 편집, 인쇄, 복제, 폐기
3. 인쇄 작업: 인쇄 수량, NCode 범위, 작업자, 출력 이력
4. 펜 업로드: USB 수집 상태, 파일별 처리 결과, 재처리, 오류
5. 문서 관리: 검색·필터·다중 선택·상태 변경·내보내기
6. 문서 상세: 필기 합성 화면, OCR 값, 반복행, 오류, 수정 이력
7. 추출 작업: CSV·Excel·PDF 생성 상태와 다운로드 이력
8. 설정: 사용자, 권한, OCR, 보존 기간, 출력 규칙

### 6.1 단계별 메뉴 구조

1A 데모에서는 고객사 관리자와 현장 사용자가 같은 고객사 콘솔을 사용한다. 역할에 따라 버튼과 데이터 범위만 다르게 표시한다.

#### 6.1.1 1차 프로토타입 대시보드

현행 기준은 `/Users/neolab/Documents/ChatGPT/forms Solution/index.html`의 `customerDashboard` 화면이다. 고객사 대시보드는 복잡한 처리 현황 그래프보다 현재 규모, 최근 문서, 자주 실행하는 작업, AI OCR 사용량을 한 화면에서 확인하는 것을 우선한다.

프로토타입의 숫자와 그래프는 화면 검증용 예시 데이터다. 실제 구현에서는 로그인한 고객사 ID와 선택 기간을 모든 집계 쿼리에 적용하며, 다른 고객사의 데이터가 섞이지 않아야 한다.

프로토타입에서는 향후 본 제품 구조를 검토할 수 있도록 `고객사`와 `시스템 관리자` 대시보드 전환 예시를 함께 제공한다. 실제 서비스에서는 로그인 역할과 콘솔 주소에 따라 분리하며 고객사 사용자가 시스템 관리자 데이터를 조회할 수 없어야 한다.

고객사 대시보드:

화면은 다음 순서로 고정한다.

```text
페이지 제목·조회 기간·기준 일시
→ 상단 결합 KPI 4개
→ 최근 문서 / 빠른 실행
→ 우리 회사 AI OCR 사용량
→ 문서 사용량 상위 사용자 / 많이 사용한 양식
```

상단 조회 조건:

- 조회 기간: 오늘, 최근 7일, 최근 30일, 이번 달, 직접 설정
- 기본값: 최근 30일
- 기준 일시: 집계가 마지막으로 갱신된 날짜와 시간을 표시
- `사용자`와 `양식` 카드는 현재 시점 값이며 기간 선택의 영향을 받지 않는다.
- `문서`, `처리 필요`, 최근 문서, 사용자·양식 순위는 선택 기간을 적용한다.
- AI OCR 사용량은 카드에 표시된 과금 기간을 우선 적용한다. 현행 화면의 기본 과금 기간은 `이번 달`이다.

상단 KPI:

| 카드 | 왼쪽 값 | 오른쪽 값 | 집계 기준 |
|---|---|---|---|
| 사용자 | 전체 등록 | 활성 | 현재 고객사에 등록된 계정 / 사용 가능 상태 계정 |
| 양식 | 전체 | 인쇄 가능 | 폐기되지 않은 전체 양식 / `Printable` 양식 |
| 문서 | 전체 | 완료 | 선택 기간에 등록된 문서 / 최종 확정된 문서 |
| 처리 필요 | 작성 | 확인 필요 필드 | 필기 데이터가 존재하는 작성 상태 문서 / OCR 검수가 필요한 필드 합계 |

- 카드 하나에 두 값을 결합하되, 제목·값·단위를 분리해 읽을 수 있어야 한다.
- 전체 고객사 합계, 전체 획 수, 예상 AI 비용은 고객사 화면에 표시하지 않는다.
- 상단 KPI 카드는 현황 요약이며 현행 프로토타입에서는 클릭 이동 기능을 제공하지 않는다.

최근 문서:

- 문서번호, 양식명, 소유자, 상태, 최근 일시를 표시한다.
- 최근 일시 내림차순으로 최대 3건을 표시한다.
- 상태는 문서 조회와 동일한 `인쇄`, `작성`, `완료`를 사용한다.
- `전체 문서 보기`를 누르면 문서 조회 메뉴로 이동한다.
- 문서 처리 비율 그래프는 제공하지 않고 최근 문서 표를 첫 업무 영역에 배치한다.

빠른 실행:

| 기능 | 이동 결과 |
|---|---|
| 새 양식 만들기 | 필드가 없는 빈 편집기로 이동하여 PDF 업로드 상태 표시 |
| 펜 데이터 가져오기 | 문서 조회로 이동하고 스마트펜 데이터 가져오기 창 표시 |
| 양식 관리 열기 | 양식 관리 테이블로 이동 |

우리 회사 AI OCR 사용량:

- 이번 달 처리 페이지 / 월 페이지 한도와 사용률을 진행 막대로 표시한다.
- OCR 문서 수, 인식 필드 수, 성공률, 평균 처리 시간, 재처리 건수를 함께 표시한다.
- 잔여량은 `월 한도 - 처리 페이지`로 계산하며 80% 이상 사용 시 경고 색상을 적용한다.
- 데이터 테스트, JSON 확인, CSV·Excel 다운로드는 AI OCR 사용량에 포함하지 않는다.
- 같은 결과를 단순 조회하는 행위도 포함하지 않는다. 사용자가 OCR을 재실행해 외부 AI 요청이 발생한 경우에는 재처리 1회와 사용 페이지를 추가 집계한다.

사용 순위:

- `문서 사용량 상위 사용자`: 선택 기간에 등록하거나 처리한 문서 수를 사용자별로 합산하여 상위 3명을 표시한다.
- `많이 사용한 양식`: 선택 기간에 생성된 문서 수를 양식별로 합산하여 상위 3개를 표시한다.
- 사용자 순위에는 이름·소속·건수, 양식 순위에는 양식명·양식 ID·건수를 표시한다.
- 동률이면 최근 사용 일시가 더 최신인 항목을 우선하고, 이후 이름 오름차순으로 정렬한다.

데이터가 없는 경우:

- KPI는 0으로 표시한다.
- 최근 문서와 순위 영역은 빈 표 대신 `선택한 기간의 데이터가 없습니다` 빈 상태를 표시한다.
- AI OCR 한도가 설정되지 않은 고객사는 사용량 숫자는 표시하되 한도와 사용률은 `미설정`으로 표시한다.

시스템 관리자 대시보드:

- 상단 KPI는 이용 고객사, 전체 등록 사용자, 활성 사용자, 최근 30일 등록 고객사, 최근 30일 해지 고객사의 5개 카드로 표시한다.
- 전체 AI OCR 문서·페이지·필드와 예상 비용
- OCR 성공률, 재처리율, 처리 오류 건수
- 최근 6개월 AI OCR 사용량 추이
- 고객사별 사용자 수, AI OCR 사용량, 성공률, 월 한도 사용률
- 사용량 80% 이상, 처리 오류 등 운영 확인 항목
- AI OCR 페이지 사용량 상위 고객사와 문서 처리량 상위 고객사를 별도로 표시한다.

화면 순서는 `KPI 5개 → 전체 AI OCR 사용량·운영 확인 → 고객사별 사용자·OCR 현황 → AI OCR·문서 처리량 상위 고객사`로 한다. 프로토타입의 고객사·시스템 관리자 전환 버튼은 두 화면을 한 번에 검토하기 위한 장치이며 실제 서비스에서는 로그인 역할과 콘솔 권한으로 분리한다.

AI 사용량은 `양식 자동 추천`과 `필기 OCR`을 구분해 집계한다. 동일 결과 조회와 CSV·Excel 생성은 OCR 사용량에 포함하지 않으며, 재처리는 새로운 AI 요청으로 별도 집계한다.

```text
홈
├─ 대시보드
│  ├─ 고객사 KPI
│  ├─ 최근 문서·빠른 실행
│  ├─ AI OCR 사용량
│  └─ 상위 사용자·양식
│
├─ 양식 관리
│  ├─ 양식 테이블·상태 필터
│  ├─ 빈 양식 업로드
│  ├─ 편집 중 양식 편집
│  ├─ 완료 양식 조회 전용 상세
│  └─ 프린트
│
└─ 문서 조회
   ├─ 문서 테이블·상태 필터
   ├─ 스마트펜 데이터 가져오기
   └─ 상태별 문서 상세
      ├─ 인쇄: 빈 원본 조회
      ├─ 작성: 필기 원본·AI OCR·검수·확정
      └─ 완료: 확정값·JSON·CSV·Excel
```

1A 프로토타입의 주 메뉴는 `대시보드`, `양식 관리`, `문서 조회` 3개만 제공한다. 스마트펜 가져오기는 별도 업로드 페이지가 아니라 문서 조회의 `데이터 가져오기` 창으로 제공하고, 데이터 출력은 별도 메뉴가 아니라 데이터 테스트 또는 완료 문서 상세의 상단 버튼으로 제공한다.

1B 데모에서 다음 메뉴를 같은 고객사 콘솔에 추가한다.

```text
인쇄·수집
├─ NCode 인쇄
│  ├─ 인쇄 작업 생성
│  └─ 인쇄 이력
├─ 스마트펜 업로드
│  ├─ USB 수집
│  └─ 업로드 로그·오류
└─ 필기 합성 PDF
   ├─ 문서별 생성
   └─ 다운로드
```

본 제품에서 시스템 관리자 콘솔과 고객사·파트너·NCode 자원·서비스 상태·과금 메뉴를 별도로 추가한다.

### 시스템 운영 콘솔

고객사·파트너, NCode 자원, 서비스 상태, 저장소, 사용량, 장애·감사 로그를 제공한다. 양식 편집과 일상 문서 검수는 고객사 콘솔의 기능이다.

시스템 운영 콘솔은 본 제품 기획에는 유지하지만 **1차 데모 화면과 개발 범위에서는 제외**한다. 1차 데모는 하나의 고객사 공간에서 고객사 관리자와 현장 사용자가 함께 사용하는 서비스로 구성한다. 로그인 역할에 따라 고객사 관리자는 양식·인쇄·전체 문서를, 현장 사용자는 본인 문서의 업로드·조회·검수 기능을 사용한다.

## 7. 기능 요구사항

### 7.1 양식 업로드 및 버전

- PDF 업로드와 페이지 미리보기를 제공한다.
- 템플릿은 `초안 → 활성 → 사용 종료` 상태를 가진다.
- PDF, 좌표, 필드, 반복행 구조가 바뀌면 기존 버전을 덮어쓰지 않고 새 버전으로 복제한다.
- 이미 인쇄된 문서는 인쇄 당시 버전에 계속 연결되어야 한다.
- 버전 목록에서 변경 요약, 생성일, 활성일, 연결 문서 수를 확인한다.
- 동일 템플릿의 번역본이나 지점별 변형은 버전이 아니라 별도 템플릿 또는 파생 템플릿으로 관리한다.

#### 7.1.1 1차 프로토타입 단순화 정책

- 프로토타입 UI에는 버전 번호, 버전 목록, 새 버전 생성 기능을 노출하지 않는다.
- 사용자 상태는 `편집 중 → 인쇄 가능` 두 단계로 표시한다.
- `편집 완료` 전에는 임시 저장과 재편집이 가능하다.
- `편집 완료`하면 양식은 `인쇄 가능` 상태가 되며 PDF, 필드, 데이터 키, 영역 좌표를 더 이상 수정할 수 없다.
- 완료된 양식의 편집 버튼은 비활성화하고 `편집 완료된 양식은 수정할 수 없습니다` 안내를 제공한다.
- 완료된 양식은 프린트와 조회만 가능하다.
- 본 제품에서는 기존 인쇄물의 좌표 호환을 위해 내부 버전 모델을 적용하지만, 이는 프로토타입 기능과 화면 범위에서 제외한다.

양식 관리 목록의 사용자 표시 상태는 프린트 가능 여부에 집중해 `Printable / Not Printable` 두 값으로 제공한다.

| 표시 상태 | 의미 | 상태 변경 |
|---|---|---|
| Printable | 신규 문서를 프린트할 수 있음 | 완료된 양식에서 사용자가 Not Printable로 변경 가능 |
| Not Printable | 신규 프린트 불가 | 편집 미완료인 경우 변경 불가, 완료 양식은 사용자가 Printable로 복구 가능 |

- `Not Printable`만으로 원인을 알 수 없으므로 `편집 미완료`, `구조 오류`, `사용자가 프린트 중지` 등의 사유를 함께 표시한다.
- 편집 중 양식은 자동으로 Not Printable이며 사용자가 강제로 Printable로 바꿀 수 없다.
- 편집 완료 및 구조 검증 통과 후 Printable로 전환할 수 있다.
- Not Printable은 기존에 인쇄한 문서의 조회·필기 수집을 막지 않고 신규 프린트만 중지한다.
- 양식 관리의 작업 열에는 프린트 버튼만 표시한다.
- Not Printable 편집 미완료 행을 누르면 편집 화면으로, 완료된 Printable 또는 사용자 중지 행을 누르면 조회 전용 양식 상세로 이동한다.
- 조회 전용 상세에서는 원본 양식, 필드 영역, 데이터 키와 데이터 테스트를 확인할 수 있지만 양식명·필드·좌표·순서를 변경할 수 없다.

양식 관리 테이블의 현행 열 순서:

```text
양식 ID → 양식명·데이터 키 → 페이지 → 필드 → 프린트 상태 → 등록일 → 수정일 → 작업
```

- 등록일과 수정일은 각각 표시하며 하나의 `Regist/Update Date` 열로 합치지 않는다.
- 작업 열에는 `프린트`만 제공한다. 편집·조회 전용 상세는 행 전체를 눌러 진입한다.
- 편집 미완료 행의 상태 선택과 프린트 버튼은 비활성화한다.
- 완료 양식의 `Printable / Not Printable` 선택은 신규 프린트 허용 여부만 변경한다.
- 1A에서는 고객 맞춤 Data Report Template, List Report Template 등록 기능을 제공하지 않는다. JSON·CSV·기본 Excel은 시스템 표준 출력으로 생성한다.

### 7.2 양식 편집기

현행 1A 편집 화면은 왼쪽 `필드 추가·문서 필드 목록`, 가운데 `PDF 캔버스`, 오른쪽 `선택 필드 속성`의 3패널 구조다. 별도 페이지 목록, 레이어 탭, 글로벌 설정, OCR 위치 탭은 제공하지 않는다. 상세 인터랙션과 데이터 모델은 `PRD_양식편집기_상세_v0.1.md`를 기준으로 한다.

현행 지원 도구:

- 선택·이동·크기 조절, 확대·축소·화면 맞춤, 실행 취소·다시 실행
- 복사·붙여넣기·삭제, 다중 선택, 왼쪽 목록 드래그 순서 변경
- 텍스트, 숫자, 날짜, 시간, 체크 판정, 선택의 6개 입력 유형
- 선택 유형의 단일·다중 설정과 옵션 표시명·저장값·옵션별 판정 영역
- 필드명, 데이터 키, 데이터 유형, 필수 여부, 유형별 형식·검증 설정
- Top·Left·Width·Height 숫자 입력과 캔버스 조작의 양방향 동기화
- 선택한 첫 행 필드들을 반복행으로 묶고 그룹명·키·최대 행·빈 행 정책 설정
- 첫 행 전체 높이를 기준으로 반복 간격 자동 계산, 반복 그룹 재편집
- PDF가 없는 신규 편집기의 빈 업로드 상태와 완료 양식의 조회 전용 상태
- 데이터 테스트에서 빈 원본·필드 구조·JSON·CSV·기본 Excel 구조 확인
- AI 자동 추천 버튼과 추천 필드 표현. 프로토타입은 모의 동작이며 실제 구현은 서버 Gemini 파이프라인으로 교체

현행 1A에서 제외하는 도구:

- 서명·이미지·바코드·도장·계산 필드
- 별도 라디오 유형. `선택` 유형의 단일 선택으로 통합한다.
- 필드 잠금·숨김, 정렬·같은 크기·간격 맞춤, 페이지 간 복제
- 정규식·조건부 표시, 필드별 모델·신뢰도·OCR 언어 설정
- 인쇄 영역과 인식 영역을 분리하는 고급 설정
- 반복행 해제, 가변 행, 헤더·합계 행, 행 추가·삭제

#### 편집 필드 상세 기능 우선순위

| 기능군 | 1차 데모 | 본 제품 확장 | 도출 근거 |
|---|---|---|---|
| 캔버스 | 확대·축소·화면 맞춤, 영역 생성·이동·크기 조절, 복사·삭제, 실행 취소, 다중 선택, 단축키 | 정렬, 같은 크기, 페이지 간 복제 | 모든 양식 |
| 기본 필드 | 텍스트 한 줄·여러 줄, 숫자, 날짜, 시간, 체크 판정, 선택 단일·다중 | 서명, 이미지, 바코드, 도장, 자동 계산 | 시설 신청서·점검표·배송 확인서 |
| 필드 속성 | 표시명, 데이터 키, 필수, 입력 형식, 숫자 범위·단위, 선택 옵션·영역 | 정규식, 길이, 조건부 표시 | 시설 신청서·설비 점검표 |
| 반복행 | 첫 행 필드 선택, 그룹 키, 높이 자동 계산, 최대 행 수, 빈 행 제외, 설정 재편집 | 가변 행, 병합 셀, 여러 반복 그룹, 소계·합계, 그룹 해제 | ITSUWA·현장 일보·작업지시서 |
| AI 추천 | 모의 추천 필드 표시, 수정·삭제 | 실제 Gemini 분석, 후보별 채택·신뢰도·재추천 | 두 핵심 샘플 |
| 시스템/NCode | 1A 편집기에서 제외 | 시스템 필드 보기, 템플릿 버전·페이지·패턴 풀 관리 | DigiDox·NCode 흐름 |
| 검증 | 필드 겹침, 키 중복, 필수 설정 누락, 페이지 밖 영역 | 참조 오류, 조건 순환, 출력 매핑 누락 | 운영 안정성 |
| 데이터 테스트 | 빈 원본, 필드 구조, JSON·CSV·기본 Excel 열 미리보기 | 고객 맞춤 Excel·API 응답 미리보기 | 추출 결과 사전 확인 |

1차 데모의 편집기는 Kotobuki와 ITSUWA를 실제로 설정할 수 있는 수준이어야 한다. 조건부 필드, 복수 반복 그룹, 계산식은 데이터 모델의 확장 지점만 확보하고 화면 구현은 후속 범위로 둔다.

AI 자동 매핑은 PDF의 문자·표 구조를 이용해 `후보 영역 + 추천 필드명 + 추천 형식`을 제안한다. 최종 데이터 키와 반복행 구조는 관리자가 확인해야 한다. 영역 안에 `[item_code]` 같은 문자열을 실제로 인쇄할 필요는 없다.

### 7.3 필드 ID와 데이터 구조

각 필드는 세 식별 계층을 갖는다.

- 표시명: 사용자가 보는 `품목코드`
- 데이터 키: 내보내기와 연동에 쓰는 `item_code`
- 내부 ID: 변경되지 않는 UUID, 화면에서는 숨김

데이터 키가 필요한 이유는 PDF 좌표, OCR 결과, 검수 화면, 데이터베이스, CSV 열, Excel 셀, API를 같은 값으로 연결하기 위해서다. 표시명을 바꾸더라도 내부 데이터 연결은 유지한다.

반복행에서는 `item_code2`처럼 필드를 복제하지 않는다.

```json
{
  "inspection_rows": [
    { "lot_no": "01", "item_code": "A-100", "qty": 20, "ok": true, "ng": false },
    { "lot_no": "02", "item_code": "B-210", "qty": 10, "ok": false, "ng": true }
  ]
}
```

편집기에서는 반복 그룹을 한 번 정의하고 행 경계 또는 행 높이와 최대 행 수를 지정한다. 시스템이 실행 시 `inspection_rows[0]`, `inspection_rows[1]`로 저장한다.

### 7.4 시스템 필드와 NCode

공통 시스템 필드는 모든 템플릿에서 동일하게 제공한다.

- `S.FORM_ID`
- `S.SEQ`
- `S.PRINT_INFO`
- `S.PAGE_NO`
- `S.OWNER_ID`
- `S.OWNER_NAME`

시스템 필드는 문서 추적용 메타데이터이며, 필요하면 PDF에 인쇄할 수 있다. NCode와 양식의 연결은 시스템 필드가 아니라 `NCode 패턴 범위 ↔ 템플릿 버전·페이지·인쇄 작업`의 별도 매핑 정보다.

### 7.5 인쇄

- 활성 템플릿 버전만 신규 인쇄할 수 있다.
- 인쇄 수량과 NCode 사용 범위를 사전에 검증한다.
- 문서 일련번호, 소유자, 출력일 등 선택한 시스템 정보를 함께 인쇄한다.
- 재인쇄는 원본과 같은 문서인지 새 문서인지 명시적으로 선택한다.
- 인쇄 작업별 생성자, 시간, 수량, 성공·실패, NCode 범위를 기록한다.
- 참고 Git 저장소나 제조사 샘플이 제공되면 라이선스, 지원 OS, 프린터 드라이버, 입력·출력 형식을 먼저 검토하고 인쇄 어댑터로 연결한다.
- 실제 연동이 불가능한 경우에도 인쇄 요청과 결과 파일 생성까지는 실제로 동작시키고, 장치 전송 단계만 모의 처리한다.

### 7.6 USB 필기 업로드

- 수집기는 연결된 펜과 미전송 파일을 표시한다.
- 파일별 진행률, 장치 ID, 크기, 수집 시간, 성공·실패 원인을 표시한다.
- 중복 업로드를 탐지하고 안전하게 재시도한다.
- 업로드 후 서버 처리 단계는 `수신 → 문서 식별 → 좌표 매핑 → OCR → 검수 대기`로 표시한다.
- 식별 불가 NCode, 알 수 없는 버전, 페이지 누락, 손상 파일은 오류함으로 보낸다.
- 참고 Git 저장소 또는 펜 SDK가 제공되면 장치 탐색, 파일 읽기, 전송 완료 처리, 재시도 규칙을 검토하여 실제 수집기를 구현한다.
- 브라우저가 USB 장치를 직접 읽는 방식과 별도 PC 수집 프로그램 방식은 지원 브라우저, OS, 드라이버와 SDK 제약을 확인한 뒤 결정한다.

#### 7.6.1 1A 프로토타입 스마트펜 가져오기

- 별도 업로드 메뉴나 페이지를 만들지 않고 문서 조회 오른쪽 위의 `데이터 가져오기`로 창을 연다.
- 창의 왼쪽에는 연결된 펜 ID·MAC·배터리와 저장된 필기 요약을 표시한다.
- 오른쪽에는 스마트펜에서 가져올 필기 원본과 페이지 썸네일을 표시한다.
- `선택한 페이지 가져오기`는 현재 선택한 페이지 1장만 문서 목록에 등록한다.
- `전체 페이지 가져오기`는 펜에 저장된 모든 페이지를 각각 문서 목록에 등록한다.
- 등록 직후 문서 상태는 `작성`이며 문서 조회 목록 상단에 표시한다.
- 프로토타입은 연결 펜 1개와 3페이지 샘플을 이용한 모의 동작이다. 실제 USB 연결 방식은 SDK·Git 샘플 확인 후 같은 화면에 어댑터로 연결한다.

### 7.7 AI OCR과 데이터 검수

- 1차 데모부터 실제 AI API를 서버에서 호출한다. API 제공자는 교체 가능하도록 OCR 요청·응답을 공통 형식으로 감싼다.
- API 키는 서버 환경 변수 또는 비밀 저장소에 보관하며, 프런트엔드 코드·네트워크 응답·로그에 노출하지 않는다.
- 전송 대상은 문서 전체가 아니라 가능한 한 필드별 잘라낸 이미지로 제한하고, 고객사·문서 식별정보는 최소화한다.
- 필드 유형과 언어에 맞는 OCR 모델 또는 설정을 적용한다.
- 체크·라디오, 짧은 문자, 숫자, 날짜, 장문, 표 반복행을 서로 다른 방식으로 처리할 수 있다.
- 결과값, 신뢰도, 원본 필기 이미지, 처리 모델·버전을 저장한다.
- 낮은 신뢰도, 필수값 누락, 형식 오류, 선택 충돌을 검수 대상으로 표시한다.
- 사용자는 원본 위의 필드 영역을 선택하고 값을 수정할 수 있다.
- 반복행은 행 추가·삭제·순서 변경과 열 단위 수정을 지원한다.
- 저장과 최종 확정을 분리한다. 확정 후에는 잠그고, 권한 있는 사용자만 사유를 남겨 재개방한다.
- 수정 전후 값과 작업자를 감사 이력으로 보존한다.

#### 7.7.1 AI 파이프라인 구분

AI 기능은 목적이 다른 두 파이프라인으로 분리한다.

1. **양식 설정 파이프라인**: 빈 PDF에서 라벨·표·체크박스·입력 후보 영역을 찾아 편집기 설정 초안을 만든다.
2. **필기 인식 파이프라인**: NCode 펜 데이터를 템플릿 필드와 연결하고 일본어 손글씨를 구조화 데이터로 변환한다.

두 파이프라인은 결과를 자동 확정하지 않는다. 양식 설정 결과는 고객사 관리자가, 필기 인식 결과는 검수 권한 사용자가 확인한다.

#### 7.7.2 빈 양식 자동 필드 추천 파이프라인

```text
빈 PDF 업로드
→ 페이지 이미지 렌더링 및 PDF 내장 문자·선 추출
→ Document AI로 일본어 라벨·표·셀·체크박스 후보 탐지
→ 규칙 엔진으로 라벨 옆 빈칸·밑줄·사각형·표 데이터행 계산
→ Gemini로 필드 의미·유형·데이터 키·반복행 구조 추천
→ 좌표를 PDF 기준 좌표로 정규화
→ 중복·겹침·페이지 이탈 검사
→ 편집기에 AI 추천 영역으로 표시
→ 고객사 관리자 확인·수정·확정
```

- Google Document AI Form Parser는 KVP, 표, 셀, 체크박스와 위치 탐지에 사용한다.
- Gemini는 일본어 라벨의 의미, 필드 유형, 영문 데이터 키와 반복행 후보를 판단한다.
- Gemini의 이미지 바운딩 박스는 보조 후보로 사용할 수 있지만 단독 정답으로 저장하지 않는다.
- 빈 양식은 값이 채워진 서식보다 KVP 탐지 신뢰도가 낮을 수 있으므로 PDF의 선·도형 분석과 사용자 확인을 필수로 한다.
- AI 추천 결과는 `추천`, 사용자가 수정한 결과는 `확정` 상태로 구분한다.

자동 추천 결과 형식 예시는 다음과 같다.

```json
{
  "fields": [
    {
      "label": "申請者氏名",
      "suggested_key": "applicant_name",
      "type": "text",
      "language": "ja",
      "box": { "page": 1, "x": 0.24, "y": 0.13, "width": 0.48, "height": 0.06 },
      "confidence": 0.87,
      "source": ["pdf_layout", "document_ai", "gemini"]
    }
  ],
  "repeating_groups": []
}
```

좌표는 화면 크기와 무관하도록 페이지 너비·높이 기준 `0~1` 정규화 좌표로 내부 저장하고, 렌더링 시 픽셀 좌표로 변환한다.

#### 7.7.3 일본어 필기 인식 파이프라인

```text
펜 원본 수신
→ NCode로 템플릿 버전·페이지 식별
→ 펜 획을 원본 PDF 좌표계로 변환
→ 필드별 획 존재 여부와 선택 상태 판정
→ 필드 영역별 고해상도 이미지 생성
→ 일본어 OCR 실행
→ Gemini로 필드 문맥에 맞는 구조화 JSON 생성·보정
→ 형식 규칙 및 고객사 사전 대조
→ 저신뢰도·충돌 항목 검수함 등록
→ 사용자 수정·최종 확정
```

역할 분담:

| 처리 | 우선 방식 | 설명 |
|---|---|---|
| 일본어 짧은 글자·장문 | Gemini 우선, 필요 시 Document AI Enterprise OCR 병행 | 실제 필기 샘플 비교 후 기본 엔진 결정 |
| 일본어 문자와 위치 | Document AI Enterprise OCR 후보 | 일본어 손글씨와 좌표를 지원하는 전문 OCR |
| 반복행 구조화 | Gemini + JSON Schema | 행과 열 정의를 프롬프트에 함께 전달 |
| 체크·라디오 | 펜 획 좌표 규칙 우선 | 생성형 AI의 추측을 방지 |
| 빈 필드 | 필드 영역 내 획 존재 여부 | OCR 결과가 아니라 원본 펜 데이터로 판단 |
| 날짜·숫자·코드 | 서버 검증 규칙 | 허용 형식, 범위, 길이 검증 |
| 품목·거래처명 | 고객사 사전 대조 | 유사 후보를 제시하되 자동 치환 여부 설정 |

- 일본어 `한자·히라가나·가타카나·영문·숫자 혼용`을 필수 시험 항목으로 둔다.
- 일본식 날짜, 장음·촉음, 탁점, 유사 문자, 품목코드의 `0/O`, `1/I`, `5/S`를 별도 오류 유형으로 측정한다.
- 전체 페이지를 한 번에 인식하기보다 템플릿이 알고 있는 필드 영역별 이미지와 필드 정의를 함께 전달한다.
- 문맥 보정 전 원 OCR 값과 보정 후 값을 모두 저장하여 추적 가능하게 한다.

#### 7.7.4 Gemini 요청 방식

- Gemini API는 서버에서만 호출하고, 필드 유형별 JSON Schema를 강제한다.
- 기본 모델은 비용·응답속도가 적합한 안정형 Flash 계열로 시작하고 실제 샘플 평가 후 고정한다.
- 기본 모델이 낮은 신뢰도 또는 형식 오류를 반환하면 전문 OCR 또는 상위 모델로 한 번 재시도한다.
- 각 요청에 `tenant_id`나 개인정보를 직접 넣지 않고, 내부 추적 ID만 사용한다.
- 저장 항목은 제공자, 모델명, 모델 버전, 프롬프트 버전, 요청 시각, 처리 시간, 원 결과, 보정 결과, 오류 코드다.
- API 실패 시 지수 백오프 재시도 후 `AI 처리 오류` 상태로 보내며, 수동 입력 경로를 제공한다.

#### 7.7.5 AI 제공자 교체 구조

애플리케이션은 Gemini 응답 형식에 직접 종속되지 않고 다음 공통 인터페이스를 사용한다.

```text
FormDetectionProvider.detect(blankForm)
HandwritingOcrProvider.recognize(fieldImage, fieldDefinition)
StructureProvider.toSchema(ocrResult, schema)
```

1차 데모 기본 구성은 `Gemini 중심 + PDF 레이아웃 분석`으로 시작한다. Document AI는 필드 자동 탐지 또는 일본어 필기 정확도가 기준에 미달할 때 같은 Google Cloud 프로젝트 안에서 추가할 수 있도록 준비한다.

#### 7.7.6 AI 평가와 승인 기준

- Kotobuki·ITSUWA별 실제 일본어 필기 정답 데이터셋을 구축한다.
- 학습·프롬프트 조정용과 최종 평가용 샘플을 분리한다.
- 일반 필드는 문자 정확도와 완전 일치율을 측정한다.
- 반복행은 셀 정확도뿐 아니라 행 누락·행 밀림·열 혼동률을 측정한다.
- 자동 필드 추천은 영역 IoU, 필드 유형 정확도, 추천 후 관리자 수정률을 측정한다.
- 모델·프롬프트 변경 전후에 동일 평가셋을 실행하고 결과가 하락하면 배포하지 않는다.
- 목표 수치는 실제 샘플 베이스라인 측정 후 확정하며, 데모 성공 여부를 단순 시연 결과 한 건으로 판단하지 않는다.

#### 7.7.7 실제 구현용 AI 파이프라인 개요

1차 구현은 **Gemini 단독 + 서버 검증 규칙**으로 완성한다. Document AI는 정확도가 부족할 때 추가할 수 있도록 인터페이스만 준비하고 실제 호출은 1차 범위에서 제외한다.

사용자가 이해하는 흐름은 다음과 같다.

```text
[양식 만들기]
빈 PDF 업로드
→ AI 자동 추천
→ 추천 필드 확인·수정
→ 템플릿 저장

[작성 데이터 처리]
필기 원본 업로드
→ AI OCR 실행
→ 인식 결과 확인·수정
→ 최종 확정
→ JSON·CSV·Excel 다운로드
```

내부 시스템에서는 위 흐름을 두 개의 독립 작업으로 처리한다.

| 파이프라인 | 입력 | AI 역할 | 사용자 결과 |
|---|---|---|---|
| 양식 자동 추천 | 빈 PDF | 필드·선택지·반복행 후보 추천 | 편집기에 추천 영역 표시 |
| 필기 OCR | 작성된 이미지 또는 펜 획 | 일본어 필기값 구조화 | 문서 검수 화면에 인식값 표시 |

두 파이프라인의 AI 결과는 자동으로 최종 확정하지 않는다.

#### 7.7.8 1차 구현 구성

```text
브라우저
→ 폼솔루션 서버 API
→ 파일 저장소
→ PDF·이미지 처리 모듈
→ AI 작업 처리기
→ Gemini API
→ 검증 모듈
→ 데이터베이스
```

역할은 다음과 같이 분리한다.

| 구성요소 | 책임 |
|---|---|
| 브라우저 | 파일 선택, 진행 상태 표시, 추천·OCR 결과 검수 |
| 서버 API | 권한 검사, 파일 접수, AI 작업 생성, 결과 반환 |
| 파일 처리 모듈 | PDF 페이지 렌더링, 이미지 정렬, 영역 자르기 |
| AI 작업 처리기 | 프롬프트 구성, Gemini 호출, 재시도, 응답 파싱 |
| 검증 모듈 | 필수값·유형·범위·선택 충돌·반복행 검사 |
| 데이터베이스 | 템플릿, 필드, AI 작업, 원 OCR 값, 최종값 저장 |
| 출력 모듈 | 최종 확정 JSON을 CSV와 기본 Excel로 변환 |

1차 데모에서는 별도 메시지 큐 없이 데이터베이스의 `AiProcessingJob` 상태를 사용하는 간단한 백그라운드 작업으로 구현할 수 있다. 단, 화면 요청 하나가 Gemini 응답이 끝날 때까지 계속 열린 상태로 기다리는 구조는 사용하지 않는다.

#### 7.7.9 API 키와 환경 설정

Gemini API 키는 서버 환경 변수로만 관리한다.

```env
GEMINI_API_KEY=실제_API_키
GEMINI_MODEL=배포_시점에_검증한_Flash_모델
AI_PROVIDER=gemini
AI_PROMPT_VERSION=form-v1
AI_MAX_RETRY=2
AI_REQUEST_TIMEOUT_MS=60000
```

- 프런트엔드 환경 변수에 API 키를 넣지 않는다.
- 브라우저가 Gemini API를 직접 호출하지 않는다.
- API 키를 서버 응답, 오류 메시지, 로그, 분석 도구에 기록하지 않는다.
- `.env` 파일은 Git에 커밋하지 않고 `.env.example`에는 변수명만 제공한다.
- 운영 환경에서는 배포 플랫폼의 Secret 또는 Google Secret Manager를 사용한다.
- 키가 설정되지 않은 로컬 환경에서는 모의 응답을 사용할 수 있지만 화면에 `Mock AI` 상태를 표시한다.

#### 7.7.10 서버 API 계약

##### A. 빈 양식 자동 추천 시작

```http
POST /api/templates/{templateId}/ai-detection
Content-Type: application/json
```

```json
{
  "page_scope": "all",
  "language": "ko",
  "detect_repeating_groups": true
}
```

응답:

```json
{
  "job_id": "ai_job_001",
  "job_type": "form_detection",
  "status": "queued"
}
```

##### B. 필기 OCR 시작

```http
POST /api/documents/{documentId}/ai-ocr
Content-Type: application/json
```

```json
{
  "template_version_id": "form_version_001",
  "page_scope": "all",
  "force": false
}
```

응답:

```json
{
  "job_id": "ai_job_002",
  "job_type": "handwriting_ocr",
  "status": "queued"
}
```

##### C. 작업 상태 조회

```http
GET /api/ai/jobs/{jobId}
```

```json
{
  "job_id": "ai_job_002",
  "status": "review_required",
  "progress": { "completed": 18, "total": 25 },
  "result_id": "ocr_result_001",
  "error": null
}
```

##### D. AI 추천 채택

```http
POST /api/templates/{templateId}/ai-candidates/apply
```

AI 후보 중 사용자가 채택한 필드와 반복 그룹 ID만 전달한다. 채택되지 않은 후보는 실제 템플릿 필드로 저장하지 않는다.

##### E. OCR 값 저장과 최종 확정

```http
PATCH /api/documents/{documentId}/field-values
POST  /api/documents/{documentId}/confirm
```

값 수정은 `PATCH`, 전체 문서 확정은 `confirm`으로 분리한다. 최종 확정 전 검증 오류가 있으면 `409 VALIDATION_FAILED`를 반환한다.

#### 7.7.11 AI 작업 상태

```text
queued
→ preprocessing
→ processing
→ validating
→ review_required
→ completed
```

실패 흐름:

```text
processing
→ retrying
→ processing
→ failed
```

| 상태 | 화면 표시 | 사용자 동작 |
|---|---|---|
| queued | AI 작업 대기 중 | 문서 조회 가능 |
| preprocessing | 페이지와 필드 영역 준비 중 | 문서 조회 가능 |
| processing | AI가 인식 중 | 필드별 진행 상태 확인 |
| validating | 형식과 반복행 검사 중 | 잠시 대기 |
| review_required | 확인이 필요한 값 존재 | 값 검수·수정 |
| completed | 처리 완료 | 최종 결과 조회 |
| retrying | 일시 오류로 재시도 중 | 문서 조회 가능 |
| failed | AI 처리 실패 | 재실행 또는 수동 입력 |

동일한 `template_version_id + document_id + source_file_hash + prompt_version`으로 완료되지 않은 작업이 있으면 새 작업을 중복 생성하지 않고 기존 `job_id`를 반환한다.

#### 7.7.12 양식 자동 추천 상세 처리

```text
1. PDF 원본 저장
2. PDF를 페이지별 고해상도 이미지로 렌더링
3. PDF 내장 텍스트와 선·사각형 정보 추출
4. 페이지 이미지와 레이아웃 힌트를 Gemini에 전달
5. JSON Schema에 맞는 필드·선택지·반복행 후보 수신
6. 좌표를 페이지 기준 0~1 값으로 변환
7. 영역 겹침, 페이지 이탈, 중복 키 검사
8. AiCandidate로 저장
9. 편집기에 보라색 추천 영역 표시
10. 사용자가 채택·수정한 항목만 FieldDefinition으로 저장
```

Gemini가 반환해야 하는 최소 형식:

```json
{
  "fields": [
    {
      "candidate_id": "candidate_001",
      "label": "점검자",
      "suggested_key": "inspector",
      "type": "text",
      "page": 1,
      "box": { "x": 0.43, "y": 0.12, "width": 0.16, "height": 0.04 },
      "choices": [],
      "review_reasons": []
    }
  ],
  "repeating_groups": [
    {
      "label": "검사 내역",
      "suggested_key": "inspection_rows",
      "field_candidate_ids": ["candidate_010", "candidate_011"],
      "suggested_rows": 25
    }
  ]
}
```

AI가 제안한 데이터 키는 서버가 `snake_case`로 다시 정규화하고 중복 시 `_2`, `_3`을 붙인다. 사용자가 표시명을 변경해도 확정된 데이터 키는 자동으로 다시 바꾸지 않는다.

#### 7.7.13 필기 OCR 상세 처리

```text
1. 작성 원본과 템플릿 버전 연결
2. 원본 이미지 방향·크기·기울기 보정
3. 템플릿 좌표를 작성 이미지 좌표로 변환
4. 필드 영역 안에 획 또는 픽셀이 존재하는지 확인
5. 일반 필드와 반복행을 각각 OCR 묶음으로 구성
6. 필드 이미지·필드명·유형·허용 형식을 Gemini에 전달
7. 구조화 JSON 응답 파싱
8. 서버 검증 규칙 실행
9. 원본값·정규화값·검수 사유 저장
10. 문서 검수 화면에 표시
```

OCR 요청 단위:

- 일반 필드는 한 페이지에서 최대 12개 필드 이미지를 한 요청으로 묶는다.
- ITSUWA 반복행은 `행 이미지 + 열 정의`를 전달하며 기본 5행씩 묶어 처리한다.
- 장문 필드는 잘리지 않도록 개별 요청으로 분리할 수 있다.
- 요청당 이미지 수와 크기는 설정값으로 관리하고 모델 제한을 넘으면 자동 분할한다.
- 일부 묶음이 실패해도 성공한 필드 결과는 유지하고 실패한 묶음만 재실행한다.

반복행 OCR 결과 예시:

```json
{
  "document_id": "document_001",
  "raw_values": {
    "inspection_rows": [
      {
        "row": 1,
        "time": "09:00",
        "package_type": "통상",
        "inspector": "김민수",
        "foreign_object": true,
        "dirt_damage": false
      }
    ]
  },
  "review_items": []
}
```

#### 7.7.14 체크·선택·빈칸 처리

| 입력 소스 | 처리 방식 |
|---|---|
| 스마트펜 좌표 있음 | 옵션별 영역 안의 획 존재 여부로 판정 |
| 일반 이미지 업로드 | 잘라낸 옵션 이미지를 Gemini로 판정 후 검수 가능 상태로 저장 |
| 획·픽셀 없음 | 빈칸 정책에 따라 `null`, `false`, 검수 필요 중 하나 적용 |
| 단일 선택에서 2개 이상 표시 | 자동 확정하지 않고 `choice_conflict` 검수 사유 생성 |
| 다중 선택 | 선택된 옵션 저장값을 배열로 저장 |

체크박스는 페이지 전체를 Gemini가 보고 추측하게 하지 않는다. 템플릿에서 정의한 각 옵션의 작은 영역만 판정한다.

#### 7.7.15 OCR 값과 검수 데이터 구조

필드값은 하나의 문자열로 덮어쓰지 않고 다음 값을 분리한다.

```json
{
  "field_key": "inspection_date",
  "raw_ocr_value": "2026/8/19",
  "normalized_value": "2026-08-19",
  "final_value": "2026-08-19",
  "value_source": "ai",
  "review_status": "confirmed",
  "review_reasons": [],
  "model": "configured-gemini-model",
  "prompt_version": "ocr-v1"
}
```

- `raw_ocr_value`: AI가 처음 읽은 값
- `normalized_value`: 날짜·숫자·코드 규칙을 적용한 값
- `final_value`: 사용자가 수정·확정한 최종값
- `value_source`: `ai`, `user`, `stroke_rule`, `empty_rule`
- `review_reasons`: 확인이 필요한 이유 목록

Gemini가 반환한 자체 신뢰도 숫자는 참고값으로만 저장하고 자동 확정 기준으로 단독 사용하지 않는다. UI의 `확인 필요` 여부는 서버 검증 결과를 우선한다.

기본 검수 사유:

```text
required_missing
type_mismatch
number_out_of_range
invalid_date
invalid_time
unknown_choice
choice_conflict
dictionary_mismatch
row_shift_suspected
ai_retry_used
manual_review_requested
```

#### 7.7.16 프롬프트와 JSON Schema 관리

- 프롬프트를 소스 코드 여러 곳에 문자열로 흩어 놓지 않는다.
- `form-detection-v1`, `field-ocr-v1`, `repeat-row-ocr-v1`처럼 목적별 파일로 분리한다.
- 모든 Gemini 요청은 구조화 출력 JSON Schema를 사용한다.
- 응답 JSON 파싱에 실패하면 동일 모델로 한 번만 JSON 복구 재시도를 한다.
- 복구에 실패하면 상위 모델로 자동 변경하지 않고 작업을 `failed` 또는 `review_required`로 보낸다. 모델 변경은 비용 정책을 확인한 뒤 설정으로 허용한다.
- 프롬프트 버전이 바뀌면 이전 결과에 사용한 버전을 계속 조회할 수 있어야 한다.
- 프롬프트에는 실제 고객사명이나 불필요한 사용자 개인정보를 넣지 않는다.

#### 7.7.17 오류·재시도·수동 처리

| 오류 | 처리 |
|---|---|
| API 시간 초과 | 지수 백오프로 최대 2회 재시도 |
| 일시적 제한 오류 | 응답의 재시도 시간을 우선 적용 |
| 잘못된 JSON | JSON 복구 요청 1회 후 실패 처리 |
| 일부 필드 실패 | 실패한 필드 묶음만 재실행 |
| 이미지 읽기 실패 | 원본 유지, 수동 입력 제공 |
| API 키 없음 | 운영에서는 실행 차단, 로컬에서는 명시적 Mock 모드만 허용 |
| 사용자가 재실행 | 기존 결과를 삭제하지 않고 새 실행 이력 추가 |

AI 실패로 문서 원본이나 사용자가 수정한 값을 삭제하지 않는다. 수동 입력만으로도 최종 확정할 수 있어야 한다.

#### 7.7.18 AI 사용량 기록

AI 호출마다 다음을 기록한다.

```text
job_id
job_type
provider
model
prompt_version
page_count
field_count
repeat_row_count
input_token_count
output_token_count
request_count
retry_count
duration_ms
status
estimated_cost
created_at
```

같은 결과를 다시 조회하거나 JSON·CSV·Excel을 생성하는 동작은 AI 사용량으로 계산하지 않는다. 사용자가 `AI 재실행`을 누른 경우에는 새로운 AI 요청으로 기록한다.

#### 7.7.19 Claude Code 구현 순서

AI 기능을 한 번에 모두 만들지 않고 다음 순서로 구현한다.

1. 서버 환경 변수와 Gemini 클라이언트 연결
2. 공통 `AiProvider` 인터페이스와 Mock/Gemini 구현체
3. `AiProcessingJob` 저장과 상태 조회 API
4. 빈 PDF 페이지 이미지 변환
5. 양식 자동 추천 JSON Schema와 후보 오버레이 연결
6. 추천 후보 채택·수정·저장
7. 필기 이미지 업로드와 템플릿 좌표 정렬
8. 일반 필드 이미지 자르기와 OCR
9. ITSUWA 반복행 행 이미지 자르기와 묶음 OCR
10. 체크·선택 획 규칙과 이미지 업로드 대체 판정
11. 검증 규칙과 확인 필요 상태
12. OCR 값 수정·최종 확정
13. 확정 JSON 기반 CSV·Excel 생성
14. 실패·재시도·사용량 로그

각 단계는 Mock 응답으로 먼저 화면과 저장을 검증한 뒤 실제 Gemini 호출로 전환한다.

#### 7.7.20 AI 구현 완료 기준

- API 키가 브라우저 소스와 네트워크 응답에 노출되지 않는다.
- 빈 양식 AI 추천 결과가 실제 편집기 후보 영역으로 표시된다.
- 추천을 채택하지 않으면 템플릿 필드에 저장되지 않는다.
- Kotobuki 일반 필드와 ITSUWA 25개 반복행을 모두 처리한다.
- 일본어, 한글, 영문, 숫자 혼합 필기값의 원본값과 최종값을 분리해 저장한다.
- 체크·선택은 스마트펜 좌표가 있으면 좌표 규칙을 우선한다.
- 오류가 발생해도 원본 파일과 사용자 수정값이 보존된다.
- 저신뢰도 숫자 하나가 아니라 구체적인 검수 사유가 화면에 표시된다.
- 최종 확정 이후 JSON·CSV·Excel 값과 반복행 수가 일치한다.
- 동일 요청의 중복 실행과 중복 과금을 방지한다.

### 7.8 문서 조회

아래 항목은 본 제품 확장 요구사항이다. 1A 프로토타입 목록은 상태와 열을 다음처럼 단순화한다.

```text
검색: 문서 번호·양식명
상태 필터: 전체 / 인쇄 / 작성 / 완료
열: No. / 양식명·데이터 키 / 페이지 / 소유자·이메일 / 상태 / 인쇄·작성 일시 / 조회
```

- 작업 버튼 명칭은 모든 상태에서 `조회`로 통일한다.
- 표의 첫 일시는 인쇄 일시, 둘째 일시는 작성 일시이며 작성 전에는 `—`로 표시한다.
- 스마트펜 데이터를 가져오면 목록 상단에 `작성` 상태 문서로 추가하고 전체·작성 집계를 갱신한다.
- 1A 상태는 `인쇄`, `작성`, `완료` 3개다. OCR 전·처리 중·확인 필요는 작성 문서 상세 내부 상태로 표현한다.

- 양식, 버전, 상태, 작성일, 업로드일, 작성자, 키워드로 검색한다.
- 상태는 최소 `인쇄됨, 필기 수신, 처리 중, 검수 필요, 확정, 오류`를 제공한다.
- 목록에 OCR 오류 수와 반복행 수를 표시한다.
- 같은 템플릿·버전의 확정 문서를 다중 선택할 수 있다.
- 다른 데이터 구조의 문서를 함께 추출하려 할 경우 분리 추출을 안내한다.

#### 7.8.1 문서 상세·OCR 검수 화면

첨부된 DigiDox 조회 화면처럼 원본 양식과 실제 필기를 기준 화면으로 유지하되, OCR 결과 검수와 반복행 편집을 한 화면에서 수행한다.

```text
┌────────────────────────────────┬──────────────────────────┐
│ 원본 양식 + 실제 필기          │ 문서 정보 / OCR 검수     │
│                                │                          │
│ 선택한 필드 영역 강조          │ 필드명·데이터 키         │
│ 확대·축소·페이지 이동          │ OCR 원본값               │
│ 필기/영역/OCR 오버레이 전환    │ 최종값 입력               │
│                                │ 검증 상태·오류           │
│                                │ 반복행 표 편집            │
└────────────────────────────────┴──────────────────────────┘
│ 이전/다음 오류  저장  최종 확정  JSON  CSV  기본 Excel     │
└───────────────────────────────────────────────────────────┘
```

왼쪽 문서 뷰어:

- 원본 PDF 위에 업로드한 필기 이미지 또는 스마트펜 획을 합성해 표시한다.
- `원본 양식`, `실제 필기`, `필드 영역`, `OCR 확정값` 레이어를 각각 켜고 끌 수 있다.
- 필드 영역을 클릭하면 오른쪽의 해당 OCR 항목으로 이동한다.
- 오른쪽 항목을 선택하면 왼쪽에서 해당 필드 영역을 확대·강조한다.
- 다중 페이지 이동, 확대·축소, 화면 맞춤, 원본 필기 다운로드를 제공한다.
- 반복행은 선택한 행과 열의 셀을 동시에 강조한다.

오른쪽 검수 패널:

- 문서 탭: 양식명, 버전, 문서번호, 페이지, 업로드 정보, 처리 상태
- OCR 필드 탭: 표시명, 데이터 키, 필드 이미지, OCR 원본값, 최종값, 검증 결과
- 반복행 탭: 행·열 구조로 값을 표시하고 행 추가·삭제 및 셀 수정
- 이력 탭: OCR 실행, 값 수정, 확정·재처리 이력
- `전체 필드`, `검수 필요`, `미인식`, `수정됨` 필터 제공
- 이전·다음 검수 항목으로 빠르게 이동할 수 있는 키보드 및 버튼 제공

화면 상태와 행동:

1. 업로드 직후에는 실제 필기 원본을 먼저 표시한다.
2. OCR 실행 전에는 `OCR 시작` 버튼과 추출될 필드 영역을 보여준다.
3. OCR 처리 중에는 문서를 계속 조회할 수 있고 필드별 진행 상태를 표시한다.
4. OCR 완료 후 원본값과 추천 최종값을 함께 보여준다.
5. 사용자가 값을 수정하고 저장한 뒤 검증 오류가 없을 때 최종 확정한다.
6. 확정 후 같은 화면에서 JSON·CSV·기본 Excel을 확인하거나 내려받는다.

원본 필기와 OCR 데이터는 별도로 보존한다. 사용자가 OCR 값을 수정해도 왼쪽의 실제 필기 원본은 변경하지 않는다.

#### 7.8.2 2026-08-19 상태별 조회 화면 확정

문서 목록의 작업 명칭은 상태와 관계없이 `조회`로 통일한다. 조회 후 상세 화면의 기능만 상태에 따라 달라진다.

| 문서 상태 | 원본 화면 | 필드값 | AI OCR | 수정 | 최종 확정 | 다운로드 |
|---|---|---|---|---|---|---|
| 인쇄 | 빈 원본 양식과 필드 영역 | 빈 값, 조회 전용 | 불가 | 불가 | 불가 | 불가 |
| 작성·OCR 전 | 스마트펜 필기 원본 | 빈 값 | 실행 가능 | 불가 | 불가 | 불가 |
| 작성·OCR 완료 | 스마트펜 필기 원본 | 인식값 자동 적용 | 재실행 가능 | 가능 | 가능 | 불가 |
| 완료 | 스마트펜 필기 원본 | 최종 확정값, 조회 전용 | 불가 | 불가 | 완료됨 | JSON·CSV·기본 Excel 가능 |

##### 문서 상세 화면 크기·비율 — 프로토타입 그대로 구현

문서 조회에서 `조회`를 눌러 진입하는 상세 화면은 작은 중앙 모달이 아니라 상단 서비스 헤더와 주 메뉴 아래를 채우는 **전체 페이지형 상세 화면**이다. 프로토타입의 `outputDialog.page-mode`와 `test-card`를 기준으로 한다.

| 항목 | 확정값 |
|---|---|
| 권장 검수 뷰포트 | 1440×900 이상 |
| 상세 콘텐츠 최대 너비 | 1320px |
| 상세 콘텐츠 가로 너비 | `width: 100%`, 바깥 여백 최소 28px |
| 상세 콘텐츠 높이 | `calc(100vh - 162px)` |
| 상세 페이지 시작 위치 | 서비스 헤더·메뉴 아래, 화면 상단에서 118px |
| 상세 페이지 바깥 여백 | 위아래 22px, 좌우 `max(28px, (100vw - 1320px) / 2)` |
| 본문 좌우 비율 | 원본 문서 45% / OCR·데이터 55% |
| 오른쪽 상하 비율 | 인식된 필드값 58% / CSV 자동 생성 42% |
| 원본 문서 기준 크기 | 700×990px, 기본 확대율 55% |

```css
.document-detail-page {
  position: fixed;
  top: 118px;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 22px max(28px, calc((100vw - 1320px) / 2));
  background: #eef2f6;
}

.document-detail-card {
  width: 100%;
  max-width: 1320px;
  height: calc(100vh - 162px);
  margin: 0 auto;
  display: flex;
  flex-direction: column;
}

.document-detail-layout {
  flex: 1;
  min-height: 0;
  display: grid;
  grid-template-columns: 45% 55%;
}

.document-detail-data {
  min-height: 0;
  display: grid;
  grid-template-rows: minmax(250px, 58%) minmax(180px, 42%);
}
```

레이아웃 금지 사항:

- 상세 화면 전체를 `transform: scale()`로 축소하지 않는다.
- 콘텐츠를 `max-width: 900px`, `960px`, `1100px` 같은 작은 모달 너비로 제한하지 않는다.
- 원본 PDF를 작은 썸네일 카드로 표시하지 않는다.
- 왼쪽 문서와 오른쪽 필드 목록을 같은 폭의 단순 카드 두 개로 재설계하지 않는다.
- 전체 페이지 스크롤 하나로 모든 영역을 길게 늘이지 않는다. 왼쪽 원본, 오른쪽 필드 목록, CSV 표는 각 영역 내부에서 스크롤한다.

상단은 문서명·상태와 `AI OCR 실행 / JSON / CSV / Excel / 최종 확정`을 한 줄에 배치한다. 본문 왼쪽은 작성 원본, 오른쪽 위는 인식된 필드값, 오른쪽 아래는 CSV 자동 생성이다. 하단에는 `이전 화면으로`와 현재 행동 안내를 표시한다.

1440×900 수용 기준:

- 상세 카드가 화면 가로폭 대부분을 사용하고 좌우 바깥 여백이 각각 약 60px이어야 한다.
- 왼쪽 원본 영역은 약 594px, 오른쪽 데이터 영역은 약 726px이어야 한다.
- 원본 문서와 인식된 필드값·CSV 영역을 스크롤 없이 동시에 식별할 수 있어야 한다.
- 작은 팝업처럼 보이거나 주변에 과도한 빈 공간이 남으면 실패로 판정한다.

작성 문서의 AI OCR 상태 전이:

```text
OCR 전
→ AI OCR 실행
→ 처리 중
→ OCR 완료·확인 필요
→ 사용자 값 확인·수정
→ 최종 확정
→ 완료·다운로드 활성
```

- `AI OCR 실행`은 Gemini 연동 지점이며 프로토타입에서는 모의 지연과 샘플 결과로 대체한다.
- 처리 중에는 중복 실행을 막는다.
- OCR 결과는 `인식된 필드값`에 적용하고 OCR 원문과 최종 수정값은 별도로 저장한다.
- JSON, CSV, 기본 Excel 버튼은 상세 화면 오른쪽 상단에 배치한다.
- 다운로드 버튼은 데이터 테스트 또는 최종 확정 문서에서만 활성화한다.
- `검수 필요` 사용자 문구는 프로토타입에서 `확인 필요`로 표현한다.

#### 7.8.3 편집기의 데이터 테스트

편집 화면의 `데이터 테스트`는 실제 필기 OCR 검수 기능과 분리한다.

- 왼쪽에는 필기가 없는 원본 양식과 확정된 필드 영역을 표시한다.
- 기본 원본은 현재 편집 중인 PDF다. 화면 검증을 위해 이미지·PDF를 임시로 올릴 수 있지만 템플릿 원본을 교체하거나 OCR을 실행하지 않는다.
- 오른쪽에는 빈 필드값, 필드명, 데이터 키, 데이터 형식을 읽기 전용으로 표시한다.
- 필드값 행을 선택하면 원본 양식에서 대응 영역을 강조한다.
- 빈 값 기준 JSON 구조와 CSV·기본 Excel의 열 순서를 미리 확인한다.
- OCR 실행, 확인 필요 필터, 값 수정, 최종 확정 기능은 데이터 테스트에서 제공하지 않는다.
- 필드 및 CSV 열 순서 변경은 데이터 테스트가 아니라 양식 편집기의 문서 필드 목록에서 수행한다.
- 데이터 테스트는 편집 결과 확인 화면이므로 필드 정의를 변경하지 않는다.

### 7.9 JSON·CSV·Excel 데이터 출력

CSV 생성에는 사용자가 템플릿을 업로드할 필요가 없다. 필드 데이터 키를 열 이름으로 사용해 자동 생성한다.

1차 데모의 출력 원칙은 **하나의 확정된 구조화 JSON을 원천 데이터로 사용하여 CSV와 기본 Excel을 자동 생성하는 것**이다. CSV와 Excel이 각각 OCR 결과를 다시 해석해서는 안 된다.

```text
Gemini OCR 결과
→ 형식 검증 및 사용자 수정
→ 최종 확정 JSON
├─ JSON 화면 확인
├─ CSV 미리보기·다운로드
└─ 기본 Excel 자동 생성·다운로드
```

#### 7.9.1 구조화 JSON — 1차 데모 포함

- 일반 필드는 문서 루트 속성으로 저장한다.
- 반복행은 배열로 저장한다.
- 화면에서 JSON을 확인하고 복사하거나 `.json` 파일로 다운로드할 수 있다.
- OCR 원본값과 검수 후 확정값을 분리해 저장하되, 기본 출력은 확정값을 사용한다.

```json
{
  "inspection_date": "2026-08-14",
  "inspector": "山田太郎",
  "inspection_rows": [
    { "time": "09:00", "item_code": "A-100", "quantity": 20, "ok": true, "ng": false },
    { "time": "13:00", "item_code": "B-200", "quantity": 10, "ok": false, "ng": true }
  ]
}
```

#### 7.9.2 CSV 자동 생성 — 1차 데모 포함

- 단건 CSV: 일반 양식은 문서 1건을 한 행으로 출력한다.
- 다건 CSV: 선택 문서를 지정 정렬순으로 병합
- 1차 데모의 반복행 기본값은 문서 기본정보를 반복하고 반복행 1개를 CSV 1행으로 평탄화하는 방식이다.
- 열 이름은 데이터 키를 기본으로 하며, 표시명 헤더 사용 여부를 선택할 수 있다.

#### 7.9.3 기본 Excel 자동 생성 — 1차 데모 포함

- 사용자가 Excel 파일을 미리 준비하지 않아도 시스템이 `.xlsx` 파일을 생성한다.
- 데이터 구성은 CSV와 동일한 확정 데이터를 사용한다.
- 기본 서식으로 헤더 강조, 열 너비, 필터, 첫 행 고정, 날짜·숫자 형식을 적용한다.
- 일반 양식은 문서 1건을 한 행으로, 반복행 양식은 기본정보를 반복하여 반복행 1개를 한 행으로 출력한다.
- 1차 데모에서는 `Data` 시트 1개를 기본으로 하며 필요하면 `Document`와 `Rows` 2개 시트 보기로 확장할 수 있다.

ITSUWA 기본 Excel 예시:

| 검사일 | 담당자 | Row | 시간 | 품목코드 | 수량 | 합격 | 불합격 |
|---|---|---:|---|---|---:|---|---|
| 2026-08-14 | 山田太郎 | 1 | 09:00 | A-100 | 20 | O |  |
| 2026-08-14 | 山田太郎 | 2 | 13:00 | B-200 | 10 |  | O |

#### 7.9.4 고객 맞춤 Excel 템플릿 — 1차 데모 제외

- 고객이 기존 Excel 파일을 업로드하는 기능
- `B3 → inspection_date`, `B4 → inspector`처럼 셀과 필드를 연결하는 기능
- 반복 시작 셀과 열, 행 추가 방식, 스타일 복사 범위를 설정하는 기능
- 상세 Excel 템플릿과 목록 Excel 템플릿을 관리하는 기능
- 위 기능은 별도의 `Excel Template Mapping` 모듈로 정의하고 2차 범위에서 구현한다.

#### 7.9.5 후속 PDF·고급 출력

- 원본 양식 PDF: 편집·좌표 매핑·인쇄의 기준 파일로 필수
- 필기 합성 PDF: 원본 PDF 위에 필기와 선택적 시스템 정보를 합성하여 증빙용으로 자동 생성
- 별도 결과 PDF 템플릿: 원본과 다른 고객 제출용 결과지가 필요할 때만 선택 사용

본 제품에서 다중 선택 시 지원할 기본 출력은 `통합 CSV`, `기본 Excel 또는 목록 Excel`, `문서별 PDF ZIP`이다. 병합 PDF는 페이지 수와 용량을 제한한 선택 기능으로 둔다. 정렬 기준은 작성일, 문서번호, 업로드일, 사용자 지정 필드 중 선택한다.

`SRC File`은 DigiDox 명칭만으로 용도가 확정되지 않았으므로 그대로 복제하지 않는다. 원 제작사 확인 전에는 제품 UI에서 `원본 양식 PDF`로 명확히 표현한다.

## 8. 문서 상태 모델

```text
인쇄됨
  → 필기 수신
  → 처리 중
  → 검수 필요
  → 확정
  → 추출 가능

처리 실패 → 오류 → 재처리
확정 → 사유 입력 후 재검수 → 검수 필요
```

페이지 일부만 들어오면 문서는 `필기 수신` 상태를 유지하고, 완료 조건을 만족한 후 OCR로 넘기는 방식과 먼저 처리하는 방식을 템플릿별로 설정할 수 있어야 한다.

## 9. 데모, 파일럿 MVP, 정식 제품 범위

### 9.1 데모: 전체 흐름이 끊기지 않는 수직 시나리오

1차 데모의 제품적 목표는 단순히 OCR 결과를 보여주는 것이 아니다. **빈 양식을 데이터 수집 구조로 정의하고, 실제 일본어 필기를 구조화하여 검수한 뒤 JSON·CSV·기본 Excel이라는 사용 가능한 데이터 파일로 반출하는 것**이다.

반드시 실제 동작:

- PDF 업로드 및 페이지 표시
- AI 필드·반복행 후보 추천 및 추천 결과 채택·수정
- 필드 생성·이동·크기 조절·속성 편집
- 반복행 그룹 정의와 미리보기
- 템플릿 저장 및 다시 불러오기
- 작성된 필기 이미지 업로드와 원본 양식 위치 정렬
- 필드 영역별 필기 이미지 추출
- Gemini 일본어 OCR, 결과 표시, 수정, 확정
- 확정된 구조화 JSON 확인 및 JSON 파일 다운로드
- CSV 미리보기 및 다운로드
- 사용자 템플릿이 필요 없는 기본 Excel 자동 생성 및 다운로드
- 고객사 관리자와 현장 사용자 역할별 메뉴·접근 차이
- 실제 API 키를 이용한 AI OCR 요청과 결과 저장

실제 연동 우선, 외부 조건 미확보 시에만 모의 처리 허용:

- 실제 NCode 라이선스·패턴 할당과 프린터 드라이버 제어
- 실제 펜 USB 통신
- 대용량 비동기 출력

데모 대표 시나리오는 Kotobuki 신청서로 일반 필드와 다중 페이지를, ITSUWA 검사표로 반복행을 보여주는 2개 흐름으로 구성한다.

#### 9.1.1 1차 데모 포함·제외 확정표

| 기능 | 1차 데모 범위 |
|---|---|
| PDF 양식 업로드 | 포함 |
| AI 자동 필드·반복행 추천 | 포함 |
| 수동 필드 영역·속성 편집 | 포함 |
| 필드 데이터 키 설정 | 포함 |
| 반복행 정의 | 포함 |
| 템플릿 저장 | 포함 |
| 필기 이미지 업로드 | 포함 |
| 필드 영역별 필기 이미지 추출 | 포함 |
| Gemini 일본어 OCR | 포함 |
| OCR 결과 검수·수정·확정 | 포함 |
| 구조화 JSON 확인·다운로드 | 포함 |
| CSV 미리보기·다운로드 | 포함 |
| 기본 Excel 자동 생성·다운로드 | 포함 |
| 고객 Excel 템플릿 업로드 | 2차 |
| Excel 셀·필드 매핑 UI | 2차 |
| NCode 실제 생성·인쇄 | 1B 데모, SDK 확인 후 실제 연결 |
| 스마트펜 USB 실제 연동 | 1B 데모, Git·SDK 확인 후 실제 연결 |
| 필기 합성 PDF | 1B 데모 |
| 시스템 관리자 | 본 제품 기획 |
| 멀티테넌트 | 본 제품 기획 |
| 승인 워크플로 | 본 제품 기획 |

#### 9.1.2 1차 데모 성공 기준

Kotobuki와 ITSUWA 빈 양식을 AI 추천과 수동 편집으로 데이터 수집 구조화하고, 실제 일본어 필기 이미지를 인식하여 사용자가 검수·확정한 뒤 동일한 확정 데이터로 JSON·CSV·기본 Excel을 생성할 수 있어야 한다.

- JSON의 일반 필드와 반복행이 CSV 및 기본 Excel에서 누락되지 않는다.
- CSV와 Excel의 데이터 값, 반복행 수, 행 순서가 동일하다.
- ITSUWA 반복행은 행별로 평탄화되고 검사일·담당자 같은 문서 기본정보가 각 행에 반복된다.
- Kotobuki 일반 필드는 문서 1건이 데이터 1행으로 출력된다.
- OCR 오인식 값을 수정하면 JSON·CSV·Excel 모두 수정된 최종 확정값을 사용한다.
- Excel 템플릿을 업로드하지 않아도 열 헤더·필터·기본 서식이 적용된 `.xlsx` 파일을 내려받을 수 있다.

#### 9.1.3 1B 연동 데모

1A의 템플릿·OCR·검수·데이터 출력 기능을 유지한 상태에서 실제 종이·스마트펜 흐름을 연결한다.

```text
확정된 템플릿
→ NCode 생성·인쇄
→ 스마트펜 작성
→ USB 필기 수집
→ 템플릿·문서·페이지 자동 식별
→ 원본 양식 위에 실제 필기 표시
→ 필기 합성 PDF 생성
→ 1A의 OCR 검수·JSON·CSV·기본 Excel 출력으로 연결
```

1B는 별도 데모를 새로 만드는 것이 아니라 1A의 필기 이미지 업로드 입력부를 실제 NCode·USB 수집 어댑터로 확장하는 단계다.

프린터·USB 참고 Git 또는 SDK가 전달되면 다음 기준으로 1B 실제 연동 방식을 판정한다.

1. 저장소의 라이선스가 데모와 제품 개발에 사용 가능한가
2. 대상 펜·프린터 모델 및 OS와 호환되는가
3. 샘플이 실제 장치 입출력까지 포함하는가
4. NCode 패턴 생성·할당 권한이 별도로 필요한가
5. 1차 데모 일정 안에 실패·중복·재연결까지 검증 가능한가

위 조건이 충족되면 실제 연동을 포함하고, 일부만 충족되면 동일 인터페이스를 사용하는 어댑터를 먼저 만든 뒤 장치 연결만 교체한다.

### 9.2 파일럿 MVP: 실제 고객 1~2곳 운영

- 실제 NCode 인쇄 및 USB 수집기
- 고객사 격리와 역할 권한
- OCR 연동, 신뢰도, 재처리
- 템플릿 버전과 과거 인쇄물 호환
- 문서 검색, 감사 이력, 기본 운영 로그
- CSV, 합성 PDF, 기본 Excel
- 백업, 장애 복구, 업로드 중복 방지

### 9.3 정식 제품

- Bluetooth·Cradle·Wi-Fi 등 추가 수집 경로
- 상세·목록 Excel 사용자 템플릿 매핑
- 조건부 필드와 고급 검증 규칙
- 승인 워크플로, 알림, API·Webhook
- 대시보드·통계·스케줄 추출
- 파트너 관리, 과금·사용량, 다국어
- OCR 모델 관리와 고객별 학습·사전

## 10. 우선순위

### P0

PDF 업로드, 템플릿 저장, AI 필드·반복행 추천, 수동 필드 편집, 반복행 정의, 필기 이미지 업로드·정렬, 필드 이미지 추출, Gemini 일본어 OCR, OCR 검수, 문서 확정, 구조화 JSON, CSV, 기본 Excel 자동 생성.

### P1

NCode·USB 실제 연동, 다중 추출, 조건부 검증, 감사 이력, 재처리, 인쇄·업로드 로그, 역할 세분화, 출력 작업 관리, 필기 합성 PDF.

### P2

사용자 Excel 템플릿, 승인 워크플로, API·Webhook, 통계 대시보드, OCR 사전·학습, 추가 수집 경로.

## 11. 핵심 데이터 개체

- Tenant: 고객사
- User / Role: 사용자와 권한
- FormTemplate: 양식의 논리적 단위
- FormVersion: PDF와 편집 정의의 불변 버전
- Page: 페이지와 NCode 페이지 정보
- FieldDefinition: 일반 필드의 좌표·형식·검증·OCR 설정
- RepeatingGroupDefinition: 반복 그룹과 열·행 규칙
- PrintJob: 인쇄 수량, 문서, NCode 범위
- Document: 실제 작성 건
- StrokeFile: 펜 원본 데이터와 업로드 정보
- FieldValue: OCR·수정·확정 값과 신뢰도
- AiProcessingJob: AI 파이프라인 단계, 제공자·모델·프롬프트 버전, 상태와 처리 시간
- AiCandidate: 자동 추천 필드·반복 그룹의 좌표, 근거, 신뢰도, 채택 여부
- OcrResult: 원 OCR 값, 문맥 보정 값, 검증 결과, 사용자 확정 값
- Dictionary: 고객사별 품목·거래처·용어 사전과 버전
- ExportJob: 출력 형식, 대상, 정렬, 상태
- AuditLog: 중요 변경 이력

## 12. 수용 기준 초안

- 동일 필드의 표시명을 바꿔도 기존 데이터와 내보내기 연결이 끊기지 않는다.
- ITSUWA 샘플에서 반복행을 한 번만 정의해 모든 행을 구조화할 수 있다.
- 구 버전으로 인쇄된 필기가 신규 버전이 활성화된 뒤에도 구 버전 좌표로 처리된다.
- 사용자는 저신뢰도 필드만 빠르게 이동하며 검수할 수 있다.
- 최종 확정 전 검증 오류를 명확히 보여주고, 확정 후 수정에는 사유가 남는다.
- 같은 구조의 확정 문서 여러 건을 선택해 정렬된 CSV로 받을 수 있다.
- 하나의 확정 JSON에서 생성한 CSV와 기본 Excel의 필드값 및 반복행 수가 일치한다.
- ITSUWA 반복행의 기본 Excel에서 문서 기본정보가 각 행에 반복되고 행 번호와 열 순서가 보존된다.
- 사용자가 Excel 템플릿을 업로드하지 않아도 `.xlsx` 파일을 생성할 수 있다.
- 합성 PDF에서 원본 양식과 필기 위치가 시각적으로 일치한다.
- 고객사 A의 사용자와 관리자는 고객사 B의 템플릿·문서·파일을 조회할 수 없다.
- 일본어 한자·히라가나·가타카나·영문·숫자가 혼합된 필드를 처리하고 원 인식값과 보정값을 함께 확인할 수 있다.
- AI가 추천한 필드 영역은 확정 전까지 실제 템플릿 버전에 적용되지 않는다.
- Gemini 또는 Document AI 호출 실패 시 문서가 유실되지 않고 재처리하거나 수동 입력할 수 있다.
- 체크·라디오와 빈칸 여부는 가능한 경우 펜 획 데이터를 우선하여 판정한다.

## 13. 성공 지표

- 신규 양식 최초 설정 완료 시간
- AI 자동 매핑 후 관리자가 수정한 영역 비율
- 필드 및 반복행 OCR 정확도
- 일본어 문자 정확도와 필드 완전 일치율
- 반복행의 행 누락·행 밀림·열 혼동률
- 문서 1건당 평균 검수 시간
- 업로드부터 확정까지의 처리 시간
- 필기 파일 식별 실패율과 중복 업로드율
- CSV·Excel 수작업 재가공 시간 감소
- 활성 양식 수, 월 처리 문서 수, 고객사별 재사용률

## 14. 결정이 필요한 항목

1. NCode 패턴의 할당·회수·재인쇄 정책과 라이선스 제약
2. 스마트펜 USB 파일 형식, 수집 SDK, 오프라인 보관 기간
3. 일본어 실제 필기 베이스라인, 목표 정확도, Gemini 단독 또는 Document AI 병행 기준과 비용
4. 다중 페이지 문서의 완료 판단 방식
5. 반복행 빈 행, 줄 걸침, 수정선, 체크 충돌의 처리 규칙
6. CSV에서 반복행을 평탄화할 기본 방식
7. 고객별 보존 기간, 삭제, 암호화, 개인정보 마스킹 정책
8. 고객 맞춤 Excel 템플릿의 셀·반복 영역 매핑 UI는 2차 범위로 확정하되 상세·목록 템플릿을 하나의 매핑 UI로 처리할지 여부
9. DigiDox의 `SRC File`, `Group Excel`, `Group List Excel` 실제 용도
10. 원 요구사항 범위 밖이었던 AI OCR을 계약·납기 범위에 포함하는 방식

## 15. 다음 설계 산출물

양식 편집기 상세 요구사항과 Claude Code 구현 단계는 `PRD_양식편집기_상세_v0.1.md`에서 관리한다.

1. 메뉴와 화면 목록, 화면별 사용자 행동
2. Kotobuki·ITSUWA 필드 정의서
3. 반복행 편집 인터랙션 상세
4. 문서·필드·반복행 데이터 스키마
5. OCR 처리·검수·재처리 예외 흐름
6. 데모 시나리오와 샘플 데이터
7. P0 기준 화면 와이어프레임과 개발 백로그
